---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
  <link rel="shortcut icon" type="image/svg" href="bunnyburrow/public/favicon.svg">
	<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			
<style>
span.small {
  font-size: smaller;
}
</style>

				                            <style>
  .blink {
    animation: blink-animation 1s steps(2, start) infinite;
    -webkit-animation: blink-animation 1s steps(2, start) infinite;
  }

  @keyframes blink-animation {
    to {
      visibility: hidden;
    }
  }
</style>

                                    <h1> 𝑺𝒂𝒍𝒗𝒆 𝒂𝒅 𝑪𝒖𝒏𝒊𝒄𝒖𝒍𝒖𝒎 𝑳𝒆𝒑𝒐𝒓𝒊𝒏𝒖𝒎 </h1> <p class="blink">
<img class="blink" src="https://i.ibb.co/CpZcVYwD/2025-11-24-under-construction.gif" alt="2025-11-24-under-construction" border="0">
			
			
				
				
				
				
			</p>
			    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>numbest_remnant.exe</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=VT323&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #000;
            color: #fff;
            font-family: 'VT323', monospace;
            overflow-x: hidden;
            min-height: 100vh;
            cursor: crosshair;
        }

        .glitch-container {
            position: relative;
            min-height: 100vh;
            background: linear-gradient(45deg, #000, #001a3a, #000);
            animation: bgPulse 8s ease infinite;
            padding: 20px;
        }

        .profile-section {
            max-width: 800px;
            margin: 40px auto;
            background: rgba(0, 0, 0, 0.8);
            border: 1px solid #0ff;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        .profile-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 30px;
        }

        .profile-pic {
            width: 150px;
            height: 150px;
            border: 2px solid #ff00ff;
            position: relative;
            overflow: hidden;
        }

        .profile-pic img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(0.8) contrast(1.2);
        }

        .profile-pic::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, transparent, #ff00ff33);
            animation: glowPulse 4s infinite;
        }

        .nameplate {
            font-size: 2.5em;
            color: #0ff;
            text-shadow: 2px 2px #ff00ff;
            animation: glitchText 3s infinite;
        }

        .links-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 10px;
            margin: 20px 0;
        }

        .link-button {
            background: rgba(0, 255, 255, 0.1);
            border: 1px solid #0ff;
            padding: 10px;
            color: #fff;
            text-decoration: none;
            text-align: center;
            transition: all 0.3s;
        }

        .link-button:hover {
            background: rgba(255, 0, 255, 0.2);
            transform: scale(1.05);
            text-shadow: 0 0 5px #ff00ff;
        }

        .preferences {
            display: flex;
            gap: 20px;
            margin: 20px 0;
        }

        .likes, .dislikes {
            flex: 1;
            background: rgba(0, 0, 0, 0.5);
            padding: 15px;
            border: 1px solid #0ff;
        }

        .spotify-embed {
            margin: 20px 0;
            border: 1px solid #ff00ff;
            padding: 10px;
        }

        .button-collection {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            margin: 20px 0;
            padding: 10px;
            background: rgba(0, 0, 0, 0.5);
            border: 1px dashed #0ff;
        }

        .button-collection img {
            width: 88px;
            height: 31px;
            object-fit: contain;
        }

        .modal {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: rgba(0, 0, 0, 0.95);
            border: 2px solid #ff00ff;
            padding: 20px;
            z-index: 1000;
            max-width: 80%;
            max-height: 80vh;
            overflow-y: auto;
        }

        .modal.active {
            display: block;
            animation: modalGlitch 0.3s;
        }

        .modal-close {
            position: absolute;
            top: 10px;
            right: 10px;
            color: #ff00ff;
            cursor: pointer;
            font-size: 1.5em;
        }

        @keyframes modalGlitch {
            0% { transform: translate(-50%, -50%) skew(-10deg); }
            50% { transform: translate(-50%, -50%) skew(10deg); }
            100% { transform: translate(-50%, -50%) skew(0deg); }
        }

        @keyframes glitchText {
            0% { transform: skew(0deg); }
            20% { transform: skew(-3deg); color: #0ff; }
            40% { transform: skew(3deg); color: #ff00ff; }
            60% { transform: skew(0deg); }
            80% { transform: skew(2deg); color: #0ff; }
            100% { transform: skew(0deg); }
        }

        @keyframes glowPulse {
            0% { opacity: 0.3; }
            50% { opacity: 0.7; }
            100% { opacity: 0.3; }
        }
      
        .floating-entities {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .floating-gif {
            position: absolute;
            opacity: 0.3;
            filter: brightness(0.7) contrast(1.2) hue-rotate(90deg);
            animation: float-random 20s linear infinite;
        }

        @keyframes float-random {
            0% { transform: translate(0, 0) rotate(0deg); }
            50% { transform: translate(100px, -100px) rotate(180deg); }
            100% { transform: translate(0, 0) rotate(360deg); }
        }

        .hud {
            position: fixed;
            top: 20px;
            left: 20px;
            background: rgba(0, 0, 0, 0.8);
            border: 1px solid #0ff;
            padding: 10px;
            font-size: 0.8em;
            z-index: 1000;
        }

        .hud-data {
            color: #0ff;
            text-shadow: 0 0 5px #0ff;
        }

        .marquee-container {
            overflow: hidden;
            background: rgba(0, 0, 0, 0.3);
            border: 1px dashed #ff00ff;
        }

        .marquee {
            display: flex;
            animation: scroll 20s linear infinite;
        }

        @keyframes scroll {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        .secret-menu {
            position: fixed;
            right: -300px;
            top: 50%;
            transform: translateY(-50%);
            width: 300px;
            background: rgba(0, 0, 0, 0.95);
            border-left: 2px solid #ff00ff;
            padding: 20px;
            transition: right 0.3s;
            z-index: 1000;
        }

        .secret-menu.active {
            right: 0;
        }

        .secret-trigger {
            position: fixed;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 20px;
            height: 100px;
            background: #ff00ff;
            cursor: pointer;
            z-index: 999;
        }

        .tab-container {
            margin-top: 20px;
        }

        .tab-buttons {
            display: flex;
            gap: 5px;
            margin-bottom: 10px;
        }

        .tab-button {
            background: none;
            border: 1px solid #0ff;
            color: #0ff;
            padding: 5px 10px;
            cursor: pointer;
        }

        .tab-button.active {
            background: rgba(0, 255, 255, 0.2);
        }

        .tab-content {
            display: none;
            padding: 10px;
            border: 1px solid #0ff;
        }

        .tab-content.active {
            display: block;
        }

        /* Particle system */
        #particles-js {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }
    </style>
</head>
<body>
    <div id="particles-js"></div>
    
    <div class="floating-entities">
        <!-- Floating gifs will be added here via JS -->
    </div>

    <div class="hud">
        <div class="hud-data">
            SYSTEM STATUS: <span id="systemStatus">OPERATIONAL</span><br>
            ENTROPY LEVEL: <span id="entropyLevel">87.3%</span><br>
            DIGITAL DECAY: <span id="decayLevel">∞</span><br>
            VOID CONNECTION: <span id="voidStatus">STABLE</span>
        </div>
    </div>

    <div class="secret-trigger"></div>
    <div class="secret-menu">
        <h3>// hidden_thoughts.log</h3>
        <div class="tab-container">
            <div class="tab-buttons">
                <button class="tab-button active" data-tab="thoughts">思考</button>
                <button class="tab-button" data-tab="void">虚無</button>
                <button class="tab-button" data-tab="dreams">夢</button>
            </div>
            <div class="tab-content active" data-tab="thoughts">
                sometimes i wonder if we're all just NPCs in someone else's simulation... maybe the glitches in my renders are actually glitches in the matrix (⊙_⊙)
            </div>
            <div class="tab-content" data-tab="void">
                void.call(existence) -> undefined<br>
                error: consciousness.exe has stopped working
            </div>
            <div class="tab-content" data-tab="dreams">
                last night i dreamed i was a ghost in the machine, floating through endless corridors of code... it felt more real than reality tbh
            </div>
        </div>
    </div>

    <div class="glitch-container">
        <div class="profile-section">
            <div class="profile-header">
                <div class="profile-pic">
                    <img src="https://files.catbox.moe/6pme78.gif" alt="profile picture">
                </div>
                <h1 class="nameplate">♥Ƹ̵̡Ӝ̵̨̄Ʒ♥ ethephtet.𝖊𝖝𝖊 ♥Ƹ̵̡Ӝ̵̨̄Ʒ♥</h1>
            </div>

            <div class="links-section">
                <a href="#" class="link-button" onclick="showModal('discord')">Discord</a>
                <a href="#" class="link-button" onclick="showModal('github')">Github</a>
                <a href="#" class="link-button" onclick="showModal('lastfm')">Last.fm</a>
                <a href="#" class="link-button" onclick="showModal('blender')">Blender Portfolio</a>
            </div>

            <div class="preferences">
                <div class="likes">
                    <h3>⟦ LIKES ⟧</h3>
                    <p>sewerslvt, machine girl, blender, low poly aesthetics, circuit bending, VR chat worlds that make you question reality</p>
                </div>
                <div class="dislikes">
                    <h3>⟦ DISLIKES ⟧</h3>
                    <p>normie aesthetics, mortality, school, when my renders crash at 99%, people who don't understand drain gang</p>
                </div>
            </div>

            <div class="spotify-embed">
                <h3>⟦ CURRENT VIBE ⟧</h3>
                <p>playlist: //voltage_decay_volume_III//</p>
            </div>

            <div class="marquee-container">
                <div class="marquee">
                    <div class="button-collection">
                        <img src="https://files.catbox.moe/rb4qfy.gif" alt="button 1">
                        <img src="https://files.catbox.moe/twmryi.png" alt="button 2">
                        <img src="https://files.catbox.moe/xev1n0.gif" alt="button 3">
                        <img src="https://files.catbox.moe/l8juvd.png" alt="button 4">
                        <img src="https://files.catbox.moe/ws7ds1.png" alt="button 5">
                        <img src="https://files.catbox.moe/84hkez.gif" alt="button 6">
                        <img src="https://files.catbox.moe/pp4gx6.gif" alt="button 7">
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modals -->
    <div id="discordModal" class="modal">
        <span class="modal-close" onclick="hideModal('discord')">×</span>
        <h2>⟦ DISCORD ⟧</h2>
        <p>xXdigital_decay_666Xx#0000</p>
    </div>

    <div id="githubModal" class="modal">
        <span class="modal-close" onclick="hideModal('github')">×</span>
        <h2>⟦ GITHUB ⟧</h2>
        <p>github.com/digital_decay_dev</p>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/particles.js/2.0.0/particles.min.js"></script>
    <script>
        function showModal(type) {
            document.getElementById(type + 'Modal').classList.add('active');
        }

        function hideModal(type) {
            document.getElementById(type + 'Modal').classList.remove('active');
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            if (event.target.classList.contains('modal')) {
                event.target.classList.remove('active');
            }
        }

        particlesJS('particles-js', {
            particles: {
                number: { value: 80 },
                color: { value: '#0ff' },
                shape: { type: 'triangle' },
                opacity: { value: 0.5 },
                size: { value: 3 },
                line_linked: {
                    enable: true,
                    distance: 150,
                    color: '#0ff',
                    opacity: 0.4,
                    width: 1
                },
                move: {
                    enable: true,
                    speed: 2,
                    direction: 'none',
                    random: true,
                    out_mode: 'out'
                }
            }
        });

        // Floating GIFs
        const floatingGifs = [
            'https://files.catbox.moe/6pme78.gif',  // Your profile gif
            'https://files.catbox.moe/rb4qfy.gif',  // Some of your button gifs
            'https://files.catbox.moe/84hkez.gif'
        ];

        const entities = document.querySelector('.floating-entities');
        
        floatingGifs.forEach((gif, index) => {
            const img = document.createElement('img');
            img.src = gif;
            img.className = 'floating-gif';
            img.style.top = `${Math.random() * 100}%`;
            img.style.left = `${Math.random() * 100}%`;
            img.style.animationDelay = `${index * -3}s`;
            entities.appendChild(img);
        });

        // HUD updates
        setInterval(() => {
            document.getElementById('entropyLevel').textContent = 
                (Math.random() * 20 + 80).toFixed(1) + '%';
            document.getElementById('voidStatus').textContent = 
                Math.random() > 0.1 ? 'STABLE' : 'UNSTABLE';
            document.getElementById('systemStatus').textContent = 
                Math.random() > 0.05 ? 'OPERATIONAL' : 'COMPROMISED';
        }, 2000);

        // Secret menu
        document.querySelector('.secret-trigger').addEventListener('mouseenter', () => {
            document.querySelector('.secret-menu').classList.add('active');
        });
    </script>
		</main>
		<Footer />
	</body>
</html>
